const TIMER_DURATION = 5 * 60; // 5 minutes in seconds

const ROLES = {
  top: 'top',
  jungle: 'jungle',
  mid: 'mid',
  adc: 'adc',
  support: 'support'
};

module.exports = {
  TIMER_DURATION,
  ROLES
}; 
